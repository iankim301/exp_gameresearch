# 팀 공유 배포 안내

## 1. 생성 보고서만 공유하기

보고서의 `웹 공유용 HTML` 버튼을 누르면 API 키가 포함되지 않은 단일 HTML 파일을 내려받습니다. 이 파일은 정적 호스팅에 업로드해 링크로 공유할 수 있습니다.

- 공개 가능한 보고서: GitHub Pages 같은 정적 호스팅
- 사내 자료: 회사의 승인된 파일 공유 또는 정적 호스팅 서비스

## 2. 툴 자체를 팀과 공유하기

이 툴은 OpenAI API 키를 사용하는 서버가 필요합니다. `index.html`만 GitHub Pages에 올리면 검색 기능은 동작하지 않고, API 키도 안전하지 않습니다.

권장 구성은 **비공개 GitHub 저장소 + Render Web Service**입니다. `render.yaml`이 포함되어 있어 Render가 시작 명령과 필요한 환경 변수 이름을 읽습니다.

1. 이 폴더 전체를 비공개 GitHub 저장소에 올립니다. `.env`와 API 키는 올리지 않습니다.
2. Render에서 해당 저장소를 연결해 Web Service를 만듭니다.
3. 시작 명령은 `npm start`를 사용합니다.
4. Render 환경 변수에 다음 값을 등록합니다.
   - `OPENAI_API_KEY`: 실제 OpenAI API 키
   - `OPENAI_MODEL`: 선택 사항. 기본값은 `gpt-5.4-mini`
   - `APP_USERNAME`: 팀 로그인 아이디
   - `APP_PASSWORD`: 길고 고유한 팀 로그인 비밀번호
5. 배포가 끝나면 생성되는 웹 주소를 팀에 공유합니다.

`APP_USERNAME`과 `APP_PASSWORD`를 설정하면 툴 전체에 브라우저 로그인 창이 표시됩니다. API 키는 이 로그인 뒤의 서버 환경 변수에만 보관되며, 팀원 브라우저로 전송되지 않습니다.

중요: 팀 로그인은 소규모 팀용의 기본 보호 장치입니다. 외부 공개가 필요한 서비스라면 SSO, VPN 또는 IP 제한 같은 회사 표준 접근 제어를 추가하세요.
